(ns rouje-like.status
  (:require [rouje-like.entity-wrapper :as rj.e]
            [rouje-like.utils :as rj.u]
            [rouje-like.messaging :as rj.msg]
            [rouje-like.config :as rj.cfg]))

(defn add-effect-fn
  [system e-this e-from]
  )

(defn remove-effect-fn
  [system e-this e-from]
  )